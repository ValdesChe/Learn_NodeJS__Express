extends ../layout
block content
    h1 Create a new chat room
    form(action="/rooms/add", method="post", name="add_room_form")
        fieldset.form-group
            label(for="name") Name:
            input(type="text", id="name", class="form-control", name="name", placeholder="Enter a name")
            small.text-muted Give your chat room a meaningful name for people to refer to it.
        button.btn.btn-primary(type="submit") Create new chat room
        a.btn.btn-default(href="/rooms/") Cancelx